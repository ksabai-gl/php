# Copy to .env for local dev (Vite reads VITE_* variables).
# In dev, requests hit /api via the Vite proxy configured in vite.config.js.
# In prod, set VITE_API_BASE to the full URL of the Node.js API.
VITE_API_BASE=/api
VITE_API_KEY=EMP-PORTAL-LEGACY-KEY-2014
